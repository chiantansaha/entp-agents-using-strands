[project]
name = "streamlit-frontend"
version = "0.1.0"
dependencies = [
    "streamlit>=1.28.0",
    "requests>=2.31.0",
    "python-multipart>=0.0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
